% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_cumulative_shocks.R
\name{generate_cumulative_shocks}
\alias{generate_cumulative_shocks}
\title{Generate Cumulative Positive and Negative Shock Variables for NARDL Models}
\usage{
generate_cumulative_shocks(variable_names, data)
}
\arguments{
\item{variable_names}{Character vector. Names of the variables in the data
frame for which to generate cumulative shock variables. Each variable must
exist in the provided data frame.}

\item{data}{A data frame containing the time series data. Should include
all variables specified in variable_names as columns.}
}
\value{
A modified data frame with additional columns for cumulative positive
and negative shocks. For each original variable, two new columns are added:
\itemize{
\item p_{variable_name}: Cumulative positive shocks (increases from previous period)
\item n_{variable_name}: Cumulative negative shocks (decreases from previous period)
The new columns are placed immediately after their corresponding original variables.
}
}
\description{
This function creates cumulative positive and negative shock variables from
original time series data, which are essential for Nonlinear Autoregressive
Distributed Lag (NARDL) model estimation. It calculates the cumulative sums
of positive and negative changes for specified variables.
}
\examples{
\dontrun{
# Generate sample data using sim_data_generate()
sample_data <- sim_data_generate(
  b = c(0.5, 0.7, 0.3, 0.2),
  order = c(1, 1),
  size = 100,
  reps = 1
)[[1]]

# Generate cumulative shocks for specific variables
processed_data <- generate_cumulative_shocks(
  variable_names = "x1",  # Replace with actual variable names
  data = sample_data
)

# View the modified data structure
str(processed_data)
}
}
