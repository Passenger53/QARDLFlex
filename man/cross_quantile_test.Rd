% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_quantile_test.R
\name{cross_quantile_test}
\alias{cross_quantile_test}
\title{Cross-Quantile Test for ECM Model Coefficients}
\usage{
cross_quantile_test(
  estimation,
  order,
  boots,
  taus,
  test_taus,
  variable,
  term = NULL,
  joint = TRUE
)
}
\arguments{
\item{estimation}{List. Estimation results obtained from \code{coef_estimate()}.}

\item{order}{Integer vector. ARDL orders obtained from \code{order_find()}.
Length must match the number of variables, otherwise an error is raised.}

\item{boots}{Integer. Number of bootstrap replications used in estimation.
This should match the value used in \code{coef_estimate()}.}

\item{taus}{Numeric vector. All quantile levels estimated, same as the
\code{taus} parameter in \code{coef_estimate()}.}

\item{test_taus}{Numeric vector. Specific quantile levels to test for equality.
Must be a subset of \code{taus} and contain at least two distinct quantiles.}

\item{variable}{Character vector. Names of variables to test. Can include
"ECM", "Intercept", or regular variable names. Special cases:
\itemize{
\item If testing the dependent variable with term = "long", tests the ECM coefficient.
\item If testing a variable with ARDL order 0, tests the contemporaneous effect.
\item Cannot mix ECM/Intercept with regular variables in the same test.
\item If length of variable larger than 1. For example, variable = c("x1", "x2"),
then this function will test H0: beta_1(tau1) = beta_1(tau2), beta_2(tau1) = beta_2(tau2)
}}

\item{term}{Character. For regular variables, specify "long" for long-term
coefficients or "short" for short-term coefficients. Only applicable when
testing regular variables (not ECM or Intercept).}

\item{joint}{Logical. For short-term coefficients, if TRUE, tests the sum
of coefficients across lags. If FALSE, tests each lag separately.
Default is TRUE.}
}
\value{
An object of class "htest" containing Wald test results, including:
\itemize{
\item statistic: The Wald test statistic
\item p.value: The p-value for the test
\item parameter: Degrees of freedom
\item method: Name of the test
}
}
\description{
This function performs a Wald test to compare coefficients across different
quantiles in the Error Correction Model (ECM) framework. It can test for
equality of coefficients across specified quantile levels, either individually
or jointly for multiple variables. The test is particularly useful for
examining parameter stability across the conditional distribution.
}
\examples{
\dontrun{
# Generate sample data
sample_data <- sim_data_generate(
  b = c(0.5, 0.7, 0.3, 0.2),
  order = c(1, 1),
  size = 100,
  reps = 1
)[[1]]

# Find optimal lag order
optimal_order <- order_find(sample_data, selection = "BIC", export = FALSE)

# Estimate models at multiple quantiles
est_result <- coef_estimate(
  data = sample_data,
  order = optimal_order,
  taus = c(0.25, 0.5, 0.75),
  boots = 50,
  export = FALSE
)

# Test if ECM coefficient is equal across quantiles 0.25, 0.5, and 0.75
test_result <- cross_quantile_test(
  estimation = est_result,
  order = optimal_order,
  boots = 50,
  taus = c(0.25, 0.5, 0.75),
  test_taus = c(0.25, 0.5, 0.75),
  variable = "ECM",
  joint = TRUE
)

print(test_result)
}
}
