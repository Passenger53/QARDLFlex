% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/single_variable_cross_wald.R
\name{single_variable_cross_wald}
\alias{single_variable_cross_wald}
\title{Single Variable Cross-Quantile Wald Test}
\usage{
single_variable_cross_wald(
  estimation,
  order,
  boots,
  taus,
  test_taus,
  export = TRUE,
  file
)
}
\arguments{
\item{estimation}{List. Estimation results obtained from \code{coef_estimate()}.}

\item{order}{Integer vector. ARDL orders for each variable, typically obtained
from \code{order_find()}. The length must match the number of variables in the
data. The first element should be the dependent variable.}

\item{boots}{Integer. Number of bootstrap replications used in the estimation.
This should match the value used in \code{coef_estimate()}.}

\item{taus}{Numeric vector. All quantile levels estimated, same as the
\code{taus} parameter in \code{coef_estimate()}.}

\item{test_taus}{Numeric vector. Specific quantile levels to test for equality.
Must be a subset of \code{taus} and contain at least two distinct quantiles.
All values must be between 0 and 1.}

\item{export}{Logical. Whether to export the results to a file. Default is TRUE.}

\item{file}{Character. File path for exporting results. Required if export = TRUE.}
}
\value{
A tibble containing Wald test results for each variable, including:
\itemize{
\item Variable: Name of the variable tested
\item term: Type of coefficient tested ("Long", "Short", or NA for special cases)
\item Wald: Wald test statistic with significance stars (***p<0.01, **p<0.05, *p<0.1)
\item pvalue: p-value of the test
\item Tau: The quantile levels tested, concatenated as a string
}
}
\description{
This function performs Wald tests for equality of coefficients across specified
quantiles for each variable in the ECM model. It tests both long-run and
short-run coefficients separately for each variable, with special handling
for the dependent variable (ECM term) and variables with zero ARDL order.
For short-run coefficients with multiple lags, the sum of coefficients across
lags is tested.
}
\examples{
\dontrun{
# Generate sample data
sample_data <- sim_data_generate(
  b = c(0.5, 0.7, 0.3, 0.2),
  order = c(1, 1),
  size = 100,
  reps = 1
)[[1]]

# Find optimal lag order
optimal_order <- order_find(sample_data, selection = "BIC", export = FALSE)

# Estimate models at multiple quantiles
est_result <- coef_estimate(
  data = sample_data,
  order = optimal_order,
  taus = c(0.25, 0.5, 0.75),
  boots = 50,
  export = FALSE
)

# Perform cross-quantile Wald tests for all variables
wald_results <- single_variable_cross_wald(
  estimation = est_result,
  order = optimal_order,
  boots = 50,
  taus = c(0.25, 0.5, 0.75),
  test_taus = c(0.25, 0.5, 0.75),
  export = FALSE
)

# View results
print(wald_results)
}
}
