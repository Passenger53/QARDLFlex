% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_dynamic_multipliers.R
\name{all_dynamic_multipliers}
\alias{all_dynamic_multipliers}
\title{Calculate Dynamic Multipliers for All Explanatory Variables in ARDL Model}
\usage{
all_dynamic_multipliers(
  data,
  order,
  taus,
  steps,
  cumulative = FALSE,
  impulse = FALSE,
  export = TRUE,
  file = NULL
)
}
\arguments{
\item{data}{A data frame containing the time series data. If the first column
is a date variable, it will be automatically excluded from analysis.}

\item{order}{Integer vector. ARDL lag orders for each variable, typically
obtained from \code{order_find()}. The length must match the number of variables
in the data frame (excluding date).}

\item{taus}{Numeric vector. Quantile levels to estimate. All values must be
between 0 and 1. Common values include c(0.25, 0.5, 0.75) for quartiles.}

\item{steps}{Integer. Number of time periods after the shock to calculate
multipliers for. Must be a positive integer greater than 0.}

\item{cumulative}{Logical. If TRUE, returns cumulative multipliers (the sum
of multipliers up to each period). If FALSE (default), returns period-by-period
multipliers.}

\item{impulse}{Logical. Specifies the type of shock:
\itemize{
\item FALSE (default): Permanent step shock (x_t = 1 for all t ≥ 0)
\item TRUE: Temporary impulse shock (x_0 = 1, x_t = 0 for t > 0)
}}

\item{export}{Logical. Whether to export the results to a file. Default is TRUE.}

\item{file}{Character. File path for exporting results. Required if export = TRUE.}
}
\value{
A tibble containing dynamic multipliers for all variables, with columns:
\itemize{
\item variable: Name of the explanatory variable
\item tau: Quantile level at which the multiplier was calculated
\item step: Time period after the shock (0 = impact period, 1 = one period after, etc.)
\item value: The dynamic multiplier value
}
}
\description{
This function computes dynamic multipliers for all explanatory variables in an
ARDL model across specified quantile levels. It extends the \code{dynamic_multipliers()}
function to automatically handle multiple variables and quantiles, providing
a comprehensive analysis of how exogenous shocks propagate through the system
over time.
}
\examples{
\dontrun{
# Generate sample data using sim_data_generate()
sample_data <- sim_data_generate(
  b = c(0.5, 0.7, 0.3, 0.2),
  order = c(1, 1),
  size = 100,
  reps = 1
)[[1]]

# Find optimal lag order using order_find()
optimal_order <- order_find(sample_data, selection = "BIC", export = FALSE)

# Calculate dynamic multipliers for all explanatory variables
results <- all_dynamic_multipliers(
  data = sample_data,
  order = optimal_order,
  taus = c(0.25, 0.5, 0.75),
  steps = 10,
  cumulative = FALSE,
  impulse = FALSE,
  export = FALSE
)

# View results
print(results)
}
}
