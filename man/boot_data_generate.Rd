% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot_data_generate.R
\name{boot_data_generate}
\alias{boot_data_generate}
\title{Generate Bootstrap Data for Quantile ARDL Model Analysis}
\usage{
boot_data_generate(data, order, tau, boots)
}
\arguments{
\item{data}{A data frame containing the time series data. If the first column
is named "date", it will be automatically excluded from the bootstrap
generation process.}

\item{order}{Integer vector. ARDL lag orders for each variable, typically obtained
from \code{order_find()}. The length must match the number of variables in the
data frame (excluding date).}

\item{tau}{Numeric. The quantile level for the quantile regression, between 0 and 1.
Common values are 0.25, 0.5 (median), and 0.75.}

\item{boots}{Integer. The number of bootstrap samples to generate.}
}
\value{
A list of length \code{boots} containing bootstrap datasets. Each dataset
is a tibble with the same structure as the original data, including
the date column (if present).
}
\description{
This function creates bootstrap datasets for quantile ARDL model analysis by
resampling residuals from a quantile regression model and generating new
time series data based on the estimated coefficients and ARDL structure.
}
\examples{
# Generate sample data
sample_data <- sim_data_generate(
  b = c(0.5, 0.7, 0.3, 0.2),
  order = c(1, 1),
  size = 100,
  reps = 1
)[[1]]

# Find optimal lag order
optimal_order <- order_find(sample_data, selection = "BIC", export = FALSE)

# Generate bootstrap data for median (tau = 0.5) regression
boot_data <- boot_data_generate(sample_data, order = optimal_order,
                                tau = 0.5, boots = 10)

# Check structure of first bootstrap sample
str(boot_data[[1]])
}
